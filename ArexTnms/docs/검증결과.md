# v0.2 검증 결과

- Java 8 언어 수준 의미 컴파일: 23개 운영 소스 통과
- Spring/MyBatis/POM/Eclipse XML 파싱: 15개 파일 통과
- Java Mapper 메소드와 MyBatis XML statement ID 대조: 누락 0건
- VO getter/setter와 MyBatis parameter/result property 대조: 누락 0건
- MyBatis 참조 테이블과 제공 DDL 대조: 핵심 7개 테이블 모두 확인
- 전체 DDL: 42개 테이블, 37개 FK 참조 대상·컬럼 누락 0건
- 메인 JSP 및 로그인 JSP JavaScript 문법 검사: 통과
- 브라우저 DOM 기반 23개 메뉴 렌더링 및 사용자 모달·권한 매트릭스 검사: 통과
- Controller → Service → Mapper 인터페이스의 Java 메소드 호출 컴파일: 통과
- Mapper 인터페이스 26개 메소드와 XML statement ID: 양방향 누락 0건
- 초기 관리자 BCrypt 해시와 `Tnms1234!` 일치 확인: 통과
- ZIP 압축 무결성 검사: 통과

현재 작업환경에는 Maven 실행 파일과 MariaDB/Tomcat 서버가 없어 실제 의존성을 내려받는 Maven WAR 패키징 및 DB 통합 기동은 수행하지 못했습니다. 대신 Java 8 API 시그니처 검증용 스텁을 사용해 운영 소스 전체를 컴파일했고, XML·SQL·JavaScript와 계층 간 연결을 별도로 대조했습니다. Eclipse에서 Maven Update 후 Tomcat 8.5로 기동하는 절차는 `README.md`에 정리했습니다.
