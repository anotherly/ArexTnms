(function(){
  function date(offset){const d=new Date();d.setMonth(d.getMonth()+(offset||0));if(offset<0)d.setDate(1);return d.toISOString().slice(0,10)}
  function duration(sec){sec=Number(sec||0);if(sec<60)return Math.round(sec)+'초';return Math.floor(sec/60)+'분 '+Math.round(sec%60)+'초'}
  function screen(){return layout('reports',`<div class="grid cols-3"><div class="card"><div class="card-title">보고서 조건</div><div class="form-grid" style="grid-template-columns:110px 1fr"><div class="label">보고서 종류</div><div class="field">장애현황 보고서</div><div class="label">시작일</div><div class="field"><input id="reportStart" class="form-control" type="date" value="${date(-1)}"></div><div class="label">종료일</div><div class="field"><input id="reportEnd" class="form-control" type="date" value="${date(0)}"></div><div class="label">시스템</div><div class="field"><select id="reportSystem" class="form-control"><option value="">전체</option><option value="VMS">영상감시설비</option><option value="PBX">전화교환설비</option><option value="SCADA_SEC">SCADA 출입보안설비</option><option value="EMS_PIDS">행선안내설비</option><option value="EMS_TX">전송설비</option></select></div></div><div style="margin-top:14px"><button class="btn primary" onclick="loadReport()">미리보기</button></div></div><div class="card span-2" id="reportResult"><div class="empty-row">조건을 선택하고 미리보기를 눌러 주세요.</div></div></div>`)}
  window.loadReport=async function(){try{const p=new URLSearchParams({startDate:document.getElementById('reportStart').value,endDate:document.getElementById('reportEnd').value});const sys=document.getElementById('reportSystem').value;if(sys)p.set('linkSysCd',sys);const d=await api('/report/fault-performance/data.ajax?'+p.toString()),s=d.summary||{};document.getElementById('reportResult').innerHTML=`<div class="card-title">${escapeHtml(d.condition.startDate)} ~ ${escapeHtml(d.condition.endDate)} 장애현황 보고서</div><div class="grid cols-4"><div class="card kpi"><div class="label">발생 장애</div><div class="value">${s.totalNocs||0}</div></div><div class="card kpi"><div class="label">서비스 장애</div><div class="value c-red">${s.serviceNocs||0}</div></div><div class="card kpi"><div class="label">평균 복구시간</div><div class="value">${duration(s.avgRecoverySec)}</div></div><div class="card kpi"><div class="label">조치 완료율</div><div class="value c-green">${Number(s.completeRate||0).toFixed(1)}%</div></div></div><table style="margin-top:18px"><thead><tr><th>시스템</th><th class="right">발생</th><th class="right">심각</th><th class="right">평균복구</th><th class="right">완료율</th></tr></thead><tbody>${(d.systems||[]).map(x=>`<tr><td>${escapeHtml(x.linkSysNm)}</td><td class="right">${x.totalNocs||0}</td><td class="right c-red">${x.criticalNocs||0}</td><td class="right">${duration(x.avgRecoverySec)}</td><td class="right">${Number(x.completeRate||0).toFixed(1)}%</td></tr>`).join('')}</tbody></table>`;enhanceTables(document.getElementById('reportResult'))}catch(e){showToast(e.message,true)}};
  window.TNMS_PAGE_INIT=async function(){renderPage('reports',screen());await window.loadReport()};
}());
